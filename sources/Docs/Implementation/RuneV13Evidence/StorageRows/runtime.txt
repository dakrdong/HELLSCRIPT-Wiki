PASS: all six portrait weapon tabs show their own XP/bar and points, including maximum level; separate mastery panel and saved-state row removed; at least three complete storage rows without clipping while retaining at least 90% of the prior board viewport; anchored color and multi-size lists with real input, outside dismissal and saved-state isolation; portrait stored-block-only rotation action; equal landscape board/storage widths; closed inset frames; pixel-aligned canvas; six full-UV 512px weapon icons; fixed unoccupiable root; all six rotations match card/detail/pickup; actual UI input module press/held movement/release at the rotated cell (<1px tracking); rotated placement; invalid drop retention; storage recovery; undo; draft isolation; owned IDs/count and rotation survive save/disk reload; Korean/English, landscape/portrait and 140% type. Automated macOS input, not physical mouse/mobile testing.
