HELLSCRIPT elite region centres — macOS development build, 2026-09-17

Player: Unity 6000.6.0f1 development build of a clone of the repository.
Run:    HELLSCRIPT -hellscriptRuneSmoke -hellscriptSavePath <fresh> -hellscriptScreenshots <dir>
        -screen-fullscreen 0 -screen-width 720 -screen-height 1280

Edit Mode: editmode-full.xml — the whole suite, 2727 / 2727 passed, including the new RuneEliteTests
and the rest of combat. Run on the same clone, batchmode, EditMode platform, no filter.

What the new part of the smoke did, through the real board callbacks on a fresh isolated account:

1. The existing screenshot fixture temporarily raised the account's best clear to 30 so every region
   opens. At the same point the fixture granted one extra G1 one-hex rune.
2. Read region 1's centre from the equipped weapon's own board and confirmed it is an elite.
3. Selected that rune in storage and tapped the centre hex; the placement landed and was saved.
4. Tapped the covered centre again. Portrait 720x1280, Korean, the selection note read the elite's
   name, its whole wording and "능력이 활성화되었습니다."
   Screenshot: 12-elite-centre-active-ko.png
5. Landscape 1280x720, English: the same hex read "Chain Collapse", its wording and
   "This ability is active."
   Screenshot: 13-elite-centre-active-en-landscape.png
6. Closed the board, started a real run, and confirmed the live sheet carried exactly one
   ELITE_* special and that it was this region's elite. The run was then returned to town.
7. Removed the placement, the granted rune and the raised clear, so the fixture leaves nothing behind.

Note on step 6: the sheet's runeAttack is NOT asserted to be zero here, because an earlier part of
the same smoke leaves an ordinary rune on the board's own G0 centre, which still grants attack power.
That an elite contributes no attack power and no stat line is measured in isolation by the Edit Mode
test CoveringARegionCentreCarriesItsEliteOntoTheSheetWithoutAddingAStatLine.

Smoke exit status: FAIL, for a reason that predates this work.

  smoke-result.txt lists one "Clipped" entry per captured screen. Every one of them is the same
  label, HELLSCRIPT/Global observation HUD/HUD safe area/Shield: an empty shield value whose rect is
  9.6 high while one line of its font needs 12 (13.44 vs 16 at 140% interface scale). This was
  confirmed pre-existing earlier the same day by building and running the same smoke from a clone
  with the rune changes reverted to HEAD, which reported the identical failure on all nine of its
  screens. This work adds screens, and each reports that same one label and nothing else. The shield
  label belongs to the global HUD work and was left alone.

Native macOS fixture only. This was not seen on real mobile hardware, and the balance of the six
abilities was not measured. The QA results inside HELLSCRIPT-RuneBoard-Elite-Preview-v13.zip describe
the browser mock-up and were not used as evidence for this build.
